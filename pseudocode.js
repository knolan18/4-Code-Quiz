// Define all variables for targeting the html

//Defines with questions array
// [
//   {
//     title: 'sample question',
//     choices: ['choice 1', 'choice 2','choice 3','choice 4',],
//     answer: 'answer'
//   },
// ]
// var score = 0;
// var time = questions * 15;

// clock tick function
// time --
// timeEl set text content to time
// have a coditional statement that does not let time drop below 0 and call the quiz to end

//Function To Start Quiz
// Start screen will get a class to hide it
// Quiz Screen will get the hide class removed
// var timerInterval = setInterval(call clocktick everyone 1 second);
// timeEl gets the text content of time
// call get a question

// Function to pull a question to the page
// Current Question = 0;
// pull the title element from html
// set the text content to currentquestion.title;
// Button container is set to innerHtml = "";
// currentquestion.choices.forEach(function(choice, i) {
  // var choiceBtn = document.createElement('button');
  // sett attributes for value of the button to be the choice[i]
  // choiceBtn.onclick = questionClick;
  // choicesEL.appendChild(choiceBtn)
// })

// Function for when a question is clicked
//  if(this.value !== correct answer){
//    time -= 10
//    set text content
//  }
// Question index ++
// conditional statment to check if current question index = questions array length
// if it is not at the length then get new question

// Function for end over
// Clear the time interval clearInterval(timeInterval);
// Hide the quiz element
// Show the form element 



//Write all event listeners or .onclick = function Call;
